/**
 * Foco de producto FuelChain Bolivia (feria / DEMO).
 * SoT de mensajes cortos — no diluir con pagos, tokenización o importador.
 */
export const PRODUCT_FOCUS =
  'Trazabilidad del camino: cantidad y calidad verificables en cada tramo.';

/** Tagline corto (shell, metadata, README). */
export const PRODUCT_TAGLINE =
  'Cantidad y calidad en cada tramo del camino.';

/** Pitch HSK / blockchain (evidencia del recorrido, no del litro físico). */
export const PRODUCT_CHAIN_LINE =
  'No tokenizamos el diésel. Anclamos la evidencia del recorrido en HSK para que sea verificable.';

export const PATH_STEPS =
  'Salida → control en ruta → llegada → recepción en estación';
