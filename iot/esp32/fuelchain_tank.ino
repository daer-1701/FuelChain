/*
 * FuelChain ESP32 — firmware stub (PHASE 15)
 * HACKATHON PROTOTYPE — not industrial certified equipment.
 */

void setup() {
  // WiFi + MQTT client
}

void loop() {
  // Read sensor → publish fuelchain/tanks/{tankId}/measurements
}
